Create a new Jetpack Compose UI component for: $ARGUMENTS

Follow the project's existing patterns:
1. Create `ui/components/{ComponentName}.kt`
2. Create a `@Composable` function with proper parameters (use state hoisting - pass state down, events up)
3. Use Material 3 components and the project's theme
4. Add a `@Preview` composable for Android Studio preview

Reference existing components like `FocusModeCard.kt`, `StreakCounter.kt`, or `UsageMirrorCard.kt` for patterns.