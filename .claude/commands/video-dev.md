Start the Remotion development studio for the gonull-video project.

Run `cd gonull-video && npm run dev` to open the Remotion Studio for previewing and editing video compositions.

If there are dependency issues, run `cd gonull-video && npm install` first.