Render the Remotion video to an output file.

1. Run `cd gonull-video && npm run build` to render GoNullOnboarding as MP4
2. If $ARGUMENTS contains "gif", run `cd gonull-video && npm run render-gif` instead
3. Report the output file location and size
4. If rendering fails, check for missing dependencies and suggest fixes