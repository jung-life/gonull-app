Create a new Remotion video composition for: $ARGUMENTS

Follow the project's existing patterns:
1. Create a new component in `gonull-video/src/{Name}.tsx` using React + Remotion
2. Use Remotion hooks (`useCurrentFrame`, `useVideoConfig`, `interpolate`, `spring`)
3. Register the composition in `gonull-video/src/Root.tsx` with appropriate dimensions (1080x1920 for vertical, 1920x1080 for horizontal)
4. Set fps to 30 to match existing compositions

Reference `GoNullOnboarding.tsx` as the pattern to follow. Use TypeScript with proper typing.