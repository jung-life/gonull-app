Run TypeScript type checking on the gonull-video project.

1. Run `cd gonull-video && npx tsc --noEmit`
2. Report any type errors with file, line number, and the error message
3. For each error, suggest the fix

If $ARGUMENTS is provided, focus analysis on that specific file.